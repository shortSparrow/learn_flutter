import 'package:flutter/material.dart';

class FavoritesMealsScreen extends StatelessWidget {
  const FavoritesMealsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Favorites'),
    );
  }
}
